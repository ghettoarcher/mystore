import AccountSettingsTitle from "../AccountSettingsTitle";

const Contacts = () => {
      return(
            <AccountSettingsTitle title={'Contacts'} subtitle={''}/>
      )
}

export default Contacts;